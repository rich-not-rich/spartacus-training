import { Tree } from '@angular-devkit/schematics';
import { experimental } from '@angular-devkit/core';
import { Schema as SpartacusOptions } from '../../add-spartacus/schema';
export declare function getWorkspace(host: Tree, files: string[]): {
    path: string;
    workspace: experimental.workspace.WorkspaceSchema;
};
export declare function getProjectFromWorkspace(tree: Tree, options: SpartacusOptions, files: string[]): experimental.workspace.WorkspaceProject;
